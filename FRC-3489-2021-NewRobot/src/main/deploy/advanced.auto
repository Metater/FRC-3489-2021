print(Test auto program running!)
delay(2000)
// direction, speedLeft, speedRight, time
moveSeconds(FORWARD, 60, 60, 2)
delay(1000)

moveSeconds(FORWARD, 40, 60, 2)

// async function with exit action body, having an exit body makes it async, reaching the end brace kills the thread
delay(2000)
{
    signal(signalA)
}

// wait for any of the semaphores to signal before continuing
// equivalent of or
wait(signalA|signalB)

// equivalent of and
wait(signalA&signalB)

moveClicks(FORWARD, )
